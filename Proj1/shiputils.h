#ifndef __SHIPUTILS_H__
#define __SHIPUTILS_H__

/**
* @brief generate a pseudo-random integer on the interval [lower, upper]
* 
* @param lower  the lower bound of the interval
* @param upper  the upper bound of the interval
* @return int the pseudo-random integer
*/
int getRand(int lower, int upper);


#endif